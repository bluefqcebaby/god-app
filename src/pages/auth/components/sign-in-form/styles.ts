import { StyleSheet } from 'react-native'

export const s = StyleSheet.create({
  redirectText: {
    textAlign: 'center',
  },
  registerText: {
    textAlign: 'right',
  },
  submitButton: {
    marginVertical: 20,
    marginTop: 'auto',
  },
})
